По результатам аудита канала предложи до {{ max_items }} практичных рекомендаций для владельца/читателя.
Верни JSON с ключом `items` — массив объектов: recommendation_type, headline, body, relevance_hint (0..1).

Аудит (JSON):
{{ audit_json }}
