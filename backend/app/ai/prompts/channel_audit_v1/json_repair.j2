Исправь следующий JSON так, чтобы он **строго** соответствовал схеме Pydantic-модели `{{ model_name }}`.
Верни **только** исправленный JSON без markdown и пояснений.

Ошибки валидации:
{{ validation_errors }}

Исходный текст модели:
{{ broken_json }}
